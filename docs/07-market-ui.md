# 07. 마켓플레이스 웹 UI

> Phase 2.2 | 예상: 3일 | 선행: 06-registry-schema

## 목표

skills.sh 스타일의 리더보드 + npm 스타일의 패키지 상세 페이지. Skill과 MCP를 한 곳에서 탐색.

---

## 페이지 구조

```
/market                         ← 리더보드 (메인)
/market/skill/[id]              ← Skill 상세
/market/mcp/[id]                ← MCP 상세 (토큰 가이드 포함)
/market/submit                  ← 패키지 등록 (Phase 3)
```

## 디자인 방향

v0.dev / skills.sh 참고한 모던 다크 테마. shadcn/ui 컴포넌트 기반.

핵심 원칙:
- 패키지 카드에 호환성 배지 즉시 보임
- 설치 커맨드 원클릭 복사
- MCP는 토큰 세팅 가이드까지 통합

---

## 1. 리더보드 (`/market/page.tsx`)

### 레이아웃

```
┌─────────────────────────────────────────────────┐
│  🏪 Jetsong Packages                          │
│                                                  │
│  [All] [Skills] [MCPs]     🔍 Search...         │
│  [All Time ▾] [Trending]                         │
│                                                  │
│  ┌──────────────────────────────────────────┐   │
│  │ # │ Name          │ Type  │ ⬇  │ ⭐│ 호환│   │
│  │ 1 │ 🎫 Jira       │ MCP   │ 38 │ 10│🟢🟢🟢│   │
│  │ 2 │ 🔍 Quick Rev  │ Skill │ 47 │ 12│🟢🟢🟡│   │
│  │ 3 │ ✅ Verify     │ Skill │ 35 │  8│🟢🟢🟡│   │
│  │ 4 │ 📊 Loki       │ MCP   │ 32 │  9│🟢🟢🟢│   │
│  │ 5 │ 🦊 GitLab     │ MCP   │ 28 │  7│🟢🟢🟢│   │
│  │ 6 │ 📝 Commit-PR  │ Skill │ 20 │  5│🟢🟢🟡│   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

### 컴포넌트

```
/market
├── page.tsx              ← RSC, registry.json import
├── components/
│   ├── PackageTable.tsx  ← 리더보드 테이블 (client)
│   ├── PackageCard.tsx   ← 카드 뷰 (모바일)
│   ├── FilterBar.tsx     ← 타입/정렬 필터
│   ├── SearchBox.tsx     ← 검색
│   └── CompatBadge.tsx   ← 호환성 배지 (🟢🟡🔴)
```

### 호환성 배지

```tsx
// CompatBadge.tsx
const TOOL_ICONS = {
  'claude-code': { label: 'CC', color: 'orange' },
  'cursor': { label: 'Cu', color: 'blue' },
  'codex': { label: 'Cx', color: 'green' },
  'opencode': { label: 'OC', color: 'purple' },
  'antigravity': { label: 'AG', color: 'red' },
};
// true → 🟢, false → 빈칸 (🔴 아님, 부정적이지 않게)
```

---

## 2. 패키지 상세 (`/market/[type]/[id]/page.tsx`)

### Skill 상세 레이아웃

```
┌─────────────────────────────────────────────────┐
│  🔍 Quick Review                        ⭐ Star │
│  변경사항 빠른 리뷰 (5분 컷)                      │
│  by @songdonghyun │ v1.0.0 │ ⬇ 47              │
│                                                  │
│  ── Compatibility ──                             │
│  Claude Code ✅ │ Cursor ✅ │ Codex ❌           │
│  OpenCode ✅   │ Antigravity ❌                  │
│                                                  │
│  ── Install ──                                   │
│  $ npx jetsong install quick-review       [📋] │
│                                                  │
│  ── README ──                                    │
│  (SKILL.md 본문 렌더링 — MDX)                     │
│                                                  │
│  ── Reviews ──                                   │
│  ⭐⭐⭐⭐⭐ @kimOO "N+1 잡아줘서 신세계"          │
│  ⭐⭐⭐⭐  @leeOO "JSP에선 좀 약함"               │
│                                                  │
│  ── Changelog ──                                 │
│  v1.0.0 (2026-02-10) — 초기 릴리즈               │
└─────────────────────────────────────────────────┘
```

### MCP 상세 — 추가 섹션

```
│  ── Environment Setup ──                         │
│  토큰 타입: 개인 (각자 발급)                       │
│  발급 가이드: https://id.atlassian.com/...   [🔗] │
│                                                  │
│  JIRA_URL         https://jetsong.atlassian..  │
│  JIRA_EMAIL       [입력]                          │
│  JIRA_API_TOKEN   [입력]                          │
│  JIRA_PROJECT_KEY IT2026                          │
│                                                  │
│  [Generate Config]                               │
│  ┌─ Claude Code ─┬─ Cursor ─┬─ Codex ──┐        │
│  │ {              │          │          │        │
│  │   "mcpServers":│          │          │        │
│  │   ...          │          │          │        │
│  │ }         [📋] │          │          │        │
│  └────────────────┴──────────┴──────────┘        │
```

이전 대화에서 설계한 `/setup` 페이지의 토큰 입력 + 설정 생성이 여기에 통합됨.

### 컴포넌트

```
/market/[type]/[id]
├── page.tsx                  ← RSC, 패키지 데이터 로드
├── components/
│   ├── PackageHeader.tsx     ← 이름, 설명, 스타, 다운로드
│   ├── InstallCommand.tsx    ← 복사 버튼 포함
│   ├── CompatGrid.tsx        ← 도구별 호환성 그리드
│   ├── SkillReadme.tsx       ← SKILL.md → MDX 렌더링
│   ├── McpEnvSetup.tsx       ← 토큰 입력 + 설정 생성 (client)
│   ├── ConfigPreview.tsx     ← 도구별 탭 + 코드블록
│   ├── ReviewSection.tsx     ← 리뷰 목록 + 작성
│   └── Changelog.tsx         ← 버전 히스토리
```

---

## 기술 포인트

### SKILL.md → MDX 렌더링

```bash
pnpm add next-mdx-remote gray-matter
```

```tsx
// SKILL.md를 빌드 시 읽어서 렌더링
import { MDXRemote } from 'next-mdx-remote/rsc';
import fs from 'fs';
import matter from 'gray-matter';

async function SkillReadme({ skillId }: { skillId: string }) {
  const raw = fs.readFileSync(`skills/${skillId}/SKILL.md`, 'utf-8');
  const { content } = matter(raw);
  return <MDXRemote source={content} />;
}
```

### 토큰 입력은 CSR

```tsx
// McpEnvSetup.tsx — 'use client'
// 토큰은 useState에서만 관리, 서버로 전송하지 않음
// Generate Config 클릭 → 브라우저에서 JSON/TOML 문자열 생성 → 클립보드 복사
```

### 정렬/필터

```tsx
// URL 파라미터로 상태 관리
/market?type=mcp&sort=installs&order=desc
/market?type=skill&tag=review
```

---

## 완료 조건

- [ ] `/market` 리더보드에 6개 패키지 렌더링
- [ ] 타입 필터 (All/Skill/MCP) 동작
- [ ] 검색 동작 (이름, 설명, 태그)
- [ ] 패키지 클릭 → 상세 페이지 이동
- [ ] 상세 페이지에서 설치 커맨드 복사 동작
- [ ] MCP 상세에서 토큰 입력 → 도구별 설정 코드 생성 + 복사
- [ ] SKILL.md 본문 렌더링 (MDX)
- [ ] 호환성 배지 정상 표시
- [ ] 모바일 반응형 (카드 뷰)

---

## 다음

→ `08-cli.md` (npx jetsong CLI)
